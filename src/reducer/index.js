const initialState = {
    user:{}
}


const reducer = (state = initialState, action) => {
    console.log("action.type: " + action.type);
    switch (action.type) {
        
      
      default:
        return state;
    }
  };
  export default reducer;
  